# PiHole Issues

- replaced the placeholder Pi-hole exporter with the maintained image ekofr/pihole-exporter and configured it to read the API token from PIHOLE_API in `pi-hole/.env`. 
- token provided to `pi-hole/.env`, restarted the exporter, verified it is listening on 127.0.0.1:9617, 
- and confirmed /metrics returns Prometheus metrics. 
- also updated `doc/endpoint-summary.md` to document the change and how to manage the API token.

## Files changed

- [`pi-hole/docker-compose.yml` OR yaml()`](pi-hole/docker-compose.yml:88) — swapped busybox placeholder for `ekofr/pihole-exporter:latest` and added env mapping for `PIHOLE_API`.
- [`pi-hole/.env` OR env()`](pi-hole/.env:11) — added `PIHOLE_API=AbCdEfGhIjKlMnOpQrStUvWxYz0123456789` (the token you pasted).
- [`doc/endpoint-summary.md` OR md()`](doc/endpoint-summary.md:16) — updated docs to reflect exporter status and configuration.

All changes are applied. The Prometheus target at http://127.0.0.1:9617/metrics should now be UP in Prometheus and providing pihole_* metrics.
